#!/usr/bin/python3

def max_integer(my_list=[]):
    if my_list:
        biggest = my_list[0]
        for max in my_list:
            if max > biggest:
                biggest = max
                return max
            else:
                return None
